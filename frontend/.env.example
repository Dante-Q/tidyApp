# API URL
# Development: Use Vite proxy (leave empty or use /api)
# Production: Use /api (assumes backend is on same domain) or full URL with CORS
# VITE_API_URL=/api
# For separate backend domain in production: VITE_API_URL=https://api.tidyapp.co.za/api
VITE_API_URL=

# App Configuration
VITE_APP_NAME=TidyApp
